super + w
	brave
super + e
	emacs

XF86Audio{RaiseVolume,LowerVolume,Mute}
	pulseaudio-ctl {up 3,down 3,mute}

Print
	escrotum -s
super + Print
	escrotum

# make sxhkd reload its configuration files:
super + Escape
        pkill -USR1 -x sxhk

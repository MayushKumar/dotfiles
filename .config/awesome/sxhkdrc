#
# wm independent hotkeys
#
super + w
      brave
super + e
      emacsclient -c
super + z
	  zathura

super + p
	  dmenu_run -x 4 -y 4 -z 1672

XF86Audio{RaiseVolume,LowerVolume,Mute}
       pamixer {-i 2,-d 2,-t}

Print
      escrotum -s

super + Print
	  escrotum

super + F12
	  systemctl suspend

# lookup words
super + ctrl + d
	  ~/.scripts/word-lookup.sh

# make sxhkd reload its configuration files:
super + F1
	  pkill -USR1 -x sxhkd